package test;

import display.FinalDisplay;

/**
 * 
 * Tests for display components, eventually testing if each button
 * and scrollbar, etc. functions properly.
 *
 */
public class DisplayTest {
	
	protected static FinalDisplay disp = FinalDisplay.getInstance();
}